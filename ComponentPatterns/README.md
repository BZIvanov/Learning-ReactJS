# Component Patterns

## Content of this section

- **component-composition**
- **compound-components**
- **resetting-component** - resetting component using `key` prop
- **infinite-loading**
- **polymorphic-components**
- **render-process** - move expensive JSX out of stateful components by passing it as `children`, so React can skip re-rendering heavy subtrees when state updates.
