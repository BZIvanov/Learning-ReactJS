// component names must start with a capital letter
// this is because React treats lowercase tag names as HTML tags and uppercase tag names as components
function App() {
  return <h1>Learning React</h1>;
}

export default App;
